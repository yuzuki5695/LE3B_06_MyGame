#pragma once
#include <BaseCharacter.h>

namespace MyGame {

    class PlayerMove {
    public:// メンバ関数

        void Update(MyEngine::Transform& transform, float speedMultiplier);
    private:// メンバ変数


    };
}